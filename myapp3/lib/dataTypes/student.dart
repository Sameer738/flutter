class Student {
  String name, imag;
  int age;
  
  Student(this.name, this.imag, this.age);


  setName(String name) {
    this.name = name;
  }

  setImag(String name) {
    this.imag = imag;
  }

  setAge(int age) {
    this.age = age;
  }
}
